<!DOCTYPE html>
<html>
<head>
  <title>Todo-App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<nav class="navbar navbar-light">
  <a class="navbar-brand" href="#">Todo</a>
  <% if logged_in? %>
    <form class="form-inline">
      <%= link_to logout_path, method: :delete do %>
        <button type="submit" class="btn btn-outline-secondary sign-out">Sign out</button>
        <% end %>
        <div class="navbar-text"><i class="far fa-user"></i><%= @current_user %></div>
      <% else %>
        <button type="submit" class="btn btn-outline-secondary sign-out" onclick="location.href=' <%= login_path %> '">Log
          in
        </button>
      <% end %>
    </form>
    </nav>
    <div class="container">
      <ol class="breadcrumb">
        <%= render_breadcrumbs :separator => " / " %>
      </ol>
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </div>
    </body>
    </html>
